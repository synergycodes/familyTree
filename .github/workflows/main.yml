name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - master # or your default branch name

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16' # or the version you are using

    - name: Install dependencies
      run: npm ci

    - name: Build project
      run: npm run build # Assuming your webpack build script in package.json is named 'build'

    - name: Deploy to GitHub Pages
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        branch: gh-pages
        folder: dist
